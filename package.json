{
  "name": "bakugan-arena-monorepo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "pnpm -r dev",
    "dev:apps": "pnpm -r --filter \"./apps/**\" dev",
    "build:server": "pnpm --filter bakugan-arena-server build",
    "build:server:render": "pnpm install && pnpm --filter bakugan-arena-server build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.15.0",
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "devDependencies": {
    "@types/ws": "^8.18.1"
  }
}